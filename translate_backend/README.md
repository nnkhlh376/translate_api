# Translate Backend (FastAPI)

Backend cung cấp API dịch đơn giản để dùng cho popup "Dịch nhanh" ở frontend.

## 1. Cài đặt môi trường

Từ thư mục gốc bài lab (`LAB_06`):

```powershell
cd translate_backend
python -m venv .venv
.\.venv\Scripts\Activate.ps1
pip install -r requirements.txt
